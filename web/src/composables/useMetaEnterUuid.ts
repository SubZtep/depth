import { v4 as uuidv4 } from "uuid"
import { TYPE } from "vue-toastification"
import CancellableToast from "~/components/toasts/CancellableToast.vue"

export function useMetaEnterUuid() {
  const toast = useToast()

  return {}
}