import Store from "beedle"

const initialState: State = {
  antialias: false,
  preferOffscreen: true,
}

const actions = {
  //
}

const mutations = {
  //
}

export default new Store({
  actions,
  mutations,
  initialState,
})
