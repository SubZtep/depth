name: TSDoc to wiki

on:
  push:
    branches: [main]

jobs:
  tsdoc-to-wiki:
    runs-on: ubuntu-latest

    steps:
      - uses: SubZtep/tsdoc-to-wiki-action@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
