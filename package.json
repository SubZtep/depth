{
  "name": "depth",
  "version": "0.1.0",
  "private": true,
  "description": "depth test",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm recursive run build",
    "clean": "pnpm recursive exec -- rm -rf node_modules dist",
    "dev": "pnpm recursive run dev --filter @depth/web",
    "serve": "pnpm recursive run dev --filter @depth/web",
    "lint": "pnpm recursive run lint",
    "test": "pnpm jest"
  },
  "author": "Andras Serfozo <subztep@gmail.com>",
  "license": "Unlicense",
  "repository": "github:SubZtep/depth",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "typescript": "^4.4.4"
  }
}
