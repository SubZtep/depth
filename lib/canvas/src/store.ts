// import Store from "beedle"
import Store from "@depth/statem"

const initialState = {
  running: false,
  antialias: false,
  preferOffscreen: true,
}

const actions = {
  //
}

const mutations = {
  //
}

export default new Store({
  actions,
  mutations,
  initialState,
})
